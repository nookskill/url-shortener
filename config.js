var config = {};
config.db = {};
config.webhost = 'http://localhost:3000/'; //can change to your host

// MongoDB
config.db.host = 'localhost'; // Host
config.db.name = 'url_shortener'; // Database name

module.exports = config;